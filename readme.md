# Todos
- fd transport
- Codegen
    - destructor
    - new_id handling
    - event `since`
    - generate documentation


```bash
swift run WaylandScanner client /usr/share/wayland-protocols/stable/xdg-shell/xdg-shell.xml a.txt
swift run WaylandScanner client /usr/share/wayland/wayland.xml a.txt
```